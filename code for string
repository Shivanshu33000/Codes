#include<bits/stdc++.h>
using namespace std;
/*
int main() {
 int SIZE ;
 cout << "Enter The Size Array : ";
 cin >> SIZE;
    string array[SIZE];
    cout << "Enter  elements for the string array: " << endl;
    for (int i = 0; i < SIZE; i++) {
        cout << "Element : ";
        cin >> array[i];
    }
    cout << "String array:";
    for (int i = 0; i < SIZE; i++) {
        cout << " " << array[i];
    }
}
*/
int main(){
    int n;
    cout << "Enter The Size : ";
    cin >> n;
    int array[n]; int a ; 
    for(int i = 0 ; i < n ; i++){
        cout << "Element : ";
        cin >> array[a];
    }
    cout << "The Array ";
    for(int i = 0 ; i < n ; i++){
        cout <<" "<<  array[i];
    }
}
